{
  "name": "beacon-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --environment node"
  },
  "dependencies": {
    "@netray-info/common-frontend": "^0.3.2",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.10",
    "vitest": "^2.0.0"
  }
}
